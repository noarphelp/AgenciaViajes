server:
  port: 8888  # Puerto donde corre el Config Server

spring:
  application:
    name: configserver # Nombre del servicio en Eureka
  cloud:
    config:
      server:
        git:
          uri: https://github.com/noarphelp/AgenciaViajes
          default-label: main  # Rama por defecto (puede ser "main" o "master")